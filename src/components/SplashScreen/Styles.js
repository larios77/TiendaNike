import { StyleSheet } from "react-native";

export const styled = StyleSheet.create({
  content: {
    justifyContent: "center",
    alignItems: "center",
    flex: 1,
  },
  image: {
    width: 200,
    height: 200,
  },
});
