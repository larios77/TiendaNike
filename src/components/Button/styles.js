import { StyleSheet } from "react-native";

export const styled = StyleSheet.create({
  containerBtn: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  buttonCard: {
    marginVertical: 20,
    backgroundColor: "#38468F",
    borderRadius: 6,
    padding: 8,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  txtButton: {
    color: "#fff",
    textAlign: "center",
    fontSize: 18,
  },
});
